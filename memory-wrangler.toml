name = "memory-worker"
main = "memory-worker.js"
compatibility_date = "2024-12-01"

# Workers AI binding
[ai]
binding = "AI"

# Durable Object binding
[[durable_objects.bindings]]
name = "CHAT_MEMORY"
class_name = "ChatMemory"
script_name = "memory-worker"

# Durable Object migration
[[migrations]]
tag = "v1"
new_classes = ["ChatMemory"]